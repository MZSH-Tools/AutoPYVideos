# AutoPYVideos Manager 设计文档

## 项目概述

**英文视频中文化工具**：将英文视频自动转换为中文字幕和配音
- 全程 **CPU 模式**（无 GPU 加速）
- 流程：下载 → 提取音频 → 英文语音识别 → 翻译成中文 → 中文配音 → 合成视频

## 环境配置

- **Conda 环境名**: `AutoPYVideos`
- **Python 版本**: 3.10
- **运行模式**: CPU（默认强制 CPU，不使用 GPU）
- **主要依赖**:
  - PySide6（GUI）
  - yt-dlp（YouTube 下载）
  - psutil（进程检测）
  - ffmpeg（视频处理）
  - faster-whisper（语音识别，CPU 模式，medium 模型）
  - edge-tts（微软免费 TTS 配音）

### Windows

```bash
# 安装 FFmpeg
winget install Gyan.FFmpeg

# 创建环境
conda create -n AutoPYVideos python=3.10
conda activate AutoPYVideos

# 安装依赖
pip install PySide6 yt-dlp psutil faster-whisper edge-tts torch torchaudio
```

### macOS

```bash
# 安装 FFmpeg
brew install ffmpeg

# 创建环境
conda create -n AutoPYVideos python=3.10
conda activate AutoPYVideos

# 安装 PyTorch（CPU 版本）
pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu

# 安装其他依赖
pip install PySide6 faster-whisper edge-tts yt-dlp ffmpeg-python srt pydub \
    openai deepl requests httpx aiohttp numpy scipy librosa soundfile \
    pillow pygame qdarkstyle sherpa-onnx sounddevice gtts zhconv jieba \
    plyer psutil openai-whisper anthropic google-genai \
    google-cloud-texttospeech google-api-python-client dashscope \
    transformers accelerate safetensors
```

**注意**：FFmpeg 通过系统包管理器安装（Windows 用 winget，macOS 用 Homebrew）

---

## 需求概述

独立的后台管理程序：
1. 后台常驻运行，系统托盘显示图标
2. 右键托盘图标可打开管理界面、退出
3. 管理界面支持添加 YouTube 链接、查看任务状态
4. **全自动流水线处理**：添加链接后自动下载→提取→识别→翻译→配音→合成
5. **同时只处理一个视频**，完成后自动处理下一个
6. 主界面默认选中正在处理的视频
7. 查看当前任务和历史记录

---

## 项目结构

**设计原则**：一个模块一个文件，禁止多功能写在同一文件

```
manager/
├── App.py            # 应用程序主类（单实例检测）
├── Tray.py           # 系统托盘模块
├── MainWindow.py     # 管理界面主窗口
├── Storage.py        # 存储路径管理
├── Task.py           # 任务管理（文件夹驱动）
├── Config.py         # 统一配置管理模块
├── config.json       # 统一配置文件（跟随版本控制）
├── Download.py       # 视频下载模块（yt-dlp）
├── Extract.py        # 音频提取模块（ffmpeg）
├── Recognize.py      # 语音识别模块（Faster-Whisper）
├── Translate.py      # 字幕翻译模块（Google Translate 免费 API）
├── Dubbing.py        # 配音模块（Edge-TTS + SpeedRate）
├── Merge.py          # 音视频合成模块（ffmpeg 硬字幕烧录）
└── README.md

RunManager.py         # 启动脚本
StartManager.vbs      # Windows 隐藏窗口启动脚本
```

---

## 任务状态（6 阶段流水线 + 校验）

| 状态 | 颜色 | 说明 | 完成标志文件 |
|------|------|------|--------------|
| Queued | 灰色 | 等待开始 | - |
| Validating | 蓝灰 | 校验/修复信息中（启动时） | - |
| Downloading | 蓝色 | 下载中 | `video.mp4` |
| Extracting | 紫色 | 提取音频中 | `audio.wav` |
| Recognizing | 橙色 | 识别中 | `en.srt` |
| Translating | 青色 | 翻译中 | `zh-cn.srt` |
| Dubbing | 靛蓝 | 配音中 | `zh-cn.wav` |
| Merging | 棕色 | 合成中 | `output.mp4` |
| Paused | 黄色 | 已暂停 | - |
| Ready | 绿色 | 待发布 | - |
| Published | 浅灰 | 已发布 | `info.json` 中有 `PublishUrl` |
| Failed | 红色 | 失败 | - |

**注意**：Validating 状态只在任务列表显示，不在流水线阶段中显示（属于启动时校验，非正常处理流程）

### 文件夹驱动的断点续传

**核心设计**：程序启动时遍历任务文件夹，根据存在的文件推断状态，无需全局 Tasks.json

```
状态推断逻辑（按优先级）：
1. info.json 有 PublishUrl → Published
2. output.mp4 存在 → Ready
3. zh-cn.wav 存在 → Merging
4. zh-cn.srt 存在 → Dubbing
5. en.srt 存在 → Translating
6. audio.wav 存在 → Recognizing
7. video.mp4 存在 → Extracting
8. 只有 info.json → Queued
```

### 启动时校验流程

程序启动时先遍历所有任务进行校验，完成后才开始正常处理流程：

```
启动流程：
1. Sync() 遍历所有任务文件夹
2. LoadTask() → InferStatus() 推断每个任务的进度状态
3. GetTasksNeedValidation() 筛选需要校验的任务
4. ValidateInfo() 根据状态执行不同操作：
   - 已发布任务：清理所有文件（只保留 info.json）
   - 未发布任务：检测并修复信息（标题、封面、中文标题）
5. 校验完成后启动正常处理流程
```

**校验检测项**：
| 检测项 | 条件 | 修复方式 |
|--------|------|----------|
| MissingTitle | 标题为空 | 调用 FetchVideoInfo 获取 |
| MissingThumbnail | thumbnail.jpg 不存在 | 下载封面图片 |
| ThumbnailPathInvalid | 封面存在但路径字段是 URL | 修正为本地路径 |
| MissingTitleZh | 中文标题为空或与英文相同 | 调用翻译接口 |

**重新执行菜单**：
- 根据任务实际进度置灰未到达的阶段（例如：没有视频文件则"从下载开始"不可点）
- 任何重新开始操作都会清除发布链接（PublishUrl）

---

## 缓存位置

- Windows: `%LOCALAPPDATA%/AutoPYVideos/`
- macOS/Linux: `~/.local/share/AutoPYVideos/`

```
AutoPYVideos/
└── Tasks/
    └── {时间戳Key}/              # 每个任务独立文件夹
        ├── info.json             # 任务元信息（URL、标题、作者、发布链接）
        ├── video.mp4             # 下载的原始视频（最高1080p）
        ├── video_slow.mp4        # 慢速后的视频（视频慢速模式生成）
        ├── audio.wav             # 提取的音频（16kHz 单声道）
        ├── en.srt                # 识别的英文字幕
        ├── zh-cn.srt             # 翻译后的中文字幕
        ├── bilingual.srt         # 双语字幕（中文在上，英文在下）
        ├── aligned.srt           # 对齐后的中文字幕（配音后生成）
        ├── aligned_bilingual.srt # 对齐版双语字幕（视频慢速模式生成）
        ├── zh-cn.wav             # 配音音频
        └── output.mp4            # 最终合成视频（双硬字幕烧录）
```

**info.json 结构**：
```json
{
  "Url": "https://...",
  "Title": "视频标题",
  "Author": "作者",
  "Thumbnail": "缩略图URL",
  "VideoId": "YouTube视频ID",
  "PublishUrl": "发布后的链接",
  "Error": "错误信息"
}
```

---

## 功能模块

### 1. 系统托盘 [已完成]
- [x] 托盘图标显示
- [x] 右键菜单：Open Manager / Exit
- [x] 双击打开管理界面
- [x] 关闭窗口隐藏而非退出
- [x] 首次运行弹出管理界面

### 2. 单实例管理 [已完成]
- [x] QLocalServer/QLocalSocket 进程间通信
- [x] 程序未运行时启动并弹出管理界面
- [x] 程序已运行时唤醒已有实例的管理界面
- [x] VBS 脚本隐藏命令行窗口启动

### 3. 管理界面 [已完成]
- [x] URL 搜索/添加输入框
- [x] 左侧任务列表（时间戳 + 状态，颜色标识）
- [x] 右侧任务详情面板（原作者、原标题、原链接、状态）
- [x] 原链接复制按钮
- [x] 发布链接可编辑输入框
- [x] 打开文件夹按钮
- [x] 搜索已存在链接时自动选中跳转
- [x] 打开时默认选中正在处理的任务，无则选中第一个
- [x] 刷新列表保持选中状态
- [x] 底部区域：左侧当前任务流程，右侧日志

### 4. 任务管理模块 [已完成]
- [x] 文件夹驱动的任务管理
- [x] 启动时遍历文件夹同步状态
- [x] 根据文件存在推断状态
- [x] 按时间排序（新在前）

### 5. 视频下载模块 [已完成]
- [x] YouTube 链接解析（yt-dlp）
- [x] 视频信息获取（标题、作者）
- [x] yt-dlp 下载（支持断点续传）
- [x] 下载进度回调
- [x] 代理支持（http://127.0.0.1:7890）
- [x] 分辨率限制（最高 1080p）

### 6. 自动处理流水线 [已完成]
- [x] 全自动队列处理（同时只处理一个）
- [x] 下载阶段（Download.py）
- [x] 音频提取阶段（Extract.py，ffmpeg 16kHz 单声道 wav）
- [x] 语音识别阶段（Recognize.py，Faster-Whisper，自动检测 CUDA/CPU）
- [x] 翻译阶段（Translate.py，Google Translate 免费 API）
- [x] 配音阶段（Dubbing.py，edge-tts 微软免费 TTS）
- [x] 合成阶段（Merge.py，调用 videotrans.util.help_ffmpeg.runffmpeg，硬字幕烧录 + 音视频合并）

---

## 与 videotrans 的关系

本项目基于 [pyvideotrans](https://github.com/jianchang512/pyvideotrans) 构建，复用其核心模块：

| 功能 | 调用模块 | 说明 |
|------|----------|------|
| FFmpeg 执行 | `videotrans.util.help_ffmpeg.runffmpeg` | 自动检测硬件编码器，统一错误处理 |
| 音频提取 | `runffmpeg` | 提取 16kHz 单声道 wav |
| 语音识别 | `videotrans.recognition.run` | Faster-Whisper，自动检测 CUDA |
| 翻译 | `videotrans.translator.run` | 默认 Google，失败自动回退 Microsoft |
| 配音 | `videotrans.tts.run` | Edge-TTS，支持多种 TTS 引擎 |
| 配音对齐 | `videotrans.task._rate.SpeedRate` | 配音时长自动加速对齐字幕时长 |
| 合成 | `runffmpeg` | 硬字幕烧录 + 音视频合并 |

manager 目录下的模块主要负责：
- 任务管理和队列调度（Task.py）
- GUI 界面（MainWindow.py、Tray.py）
- YouTube 下载（Download.py，yt-dlp，videotrans 无此功能）

**全部核心处理流程调用 videotrans 接口**：
- 音频提取（Extract.py → `runffmpeg`）
- 语音识别（Recognize.py → `recognition.run`）
- 翻译（Translate.py → `translator.run`）
- 配音（Dubbing.py → `tts.run` + `SpeedRate`）
- 合成（Merge.py → `runffmpeg`）

### 语音识别配置

**Recognize.py 默认配置**：
- **识别引擎**：Faster-Whisper（`recognition.FASTER_WHISPER`）
- **模型**：`small.en`（英语专用模型，速度快、准确率高）
- **语言**：`en`（英语）
- **分割方式**：`split_type=0`（整体识别，非均等分割）
- **LLM 重新断句**：关闭（`config.settings['rephrase'] = 0`）
- **自动修正**：启用（`AutoFix=True`，调用 `tools.auto_fix_srtdict()`）

**参数说明**：
| 参数 | 值 | 说明 |
|------|------|------|
| Model | `small.en` | 英语专用模型，比通用 small 更快更准 |
| SplitType | `0` | 整体识别（1=均等分割） |
| AutoFix | `True` | 自动修正字幕时间轴和文本 |
| rephrase | `0` | 不使用 LLM 重新断句 |

### 翻译配置

**Translate.py 默认配置**：
- **翻译引擎**：Google Translate（`translator.GOOGLE_INDEX`）
- **源语言**：`en`（英语）
- **目标语言**：`zh-cn`（简体中文）

### 配音配置

**Dubbing.py 设计原则**：
- **完全使用 videotrans 接口**，不实现备用方案
- 有错误直接抛出异常，不做降级处理
- 调用 `tts.run()` 生成配音，调用 `SpeedRate` 对齐时间轴

**默认配置**：
- **TTS 引擎**：Edge-TTS（`tts.EDGE_TTS`）
- **配音角色**：`晓晓(Female/CN)`
- **Voice ID**：`zh-CN-XiaoxiaoNeural`
- **语速**：`voice_rate`（来自 `config.json`，默认 `+25%`）
- **配音自动加速**：关闭（`VoiceAutorate=False`）
- **视频慢放**：启用（`VideoSlowdown=True`）

**注意**：中文多语言声音（`zh-CN-XiaoxiaoMultilingualNeural`）在 edge-tts 7.2.1 中不可用，需等待 edge-tts 修复后才能使用

### 对齐控制配置

**SpeedRate 参数**：
| 参数 | 值 | 说明 |
|------|------|------|
| shoud_audiorate | `False` | 配音加速（不勾选） |
| shoud_videorate | `True` | 视频慢速（勾选） |
| remove_silent_mid | `False` | 删除中间静音（不勾选） |
| align_sub_audio | `True` | 字幕音频对齐 |

**视频慢速模式**：
- 视频慢速后保存为 `video_slow.mp4`，原视频 `video.mp4` 保持不变
- 生成对齐版双语字幕 `aligned_bilingual.srt`（时间轴与慢速视频匹配）
- 合成时优先使用 `video_slow.mp4` 和 `aligned_bilingual.srt`

### 字幕嵌入配置

**subtitle_type 类型**（参考 `videotrans/task/taskcfg.py`）：
| 值 | 说明 |
|------|------|
| 0 | 不嵌入 |
| 1 | 硬字幕（单） |
| 2 | 软字幕（单） |
| 3 | 双硬字幕 |
| 4 | 双软字幕 |

**当前配置**：使用 **双硬字幕**（中文在上，英文在下）
- 翻译完成后调用 `MergeBilingualSrt()` 合并双语字幕
- 合成时使用 `bilingual.srt` 烧录硬字幕

### 状态检查绕过

videotrans 的 `recognition.run()` 和 `tts.run()` 内部有状态检查：
```python
# recognition
if config.exit_soft or (config.current_status != 'ing' and config.box_recogn != 'ing'):
    return

# tts
if config.exit_soft or (config.current_status != 'ing' and config.box_tts != 'ing'):
    return
```

解决方案：调用前临时设置 `config.box_recogn = 'ing'` 或 `config.box_tts = 'ing'`，调用后恢复

### 配音对齐（SpeedRate）

配音与字幕时长对齐使用 `videotrans.task._rate.SpeedRate` 类：

**问题**：中文配音时长通常比英文字幕时长更长，导致"话赶话"

**解决方案**：
- `VoiceAutorate=True`（默认启用）：配音自动加速，确保配音在字幕时间内播放完成
- 使用 `atempo` 或 `rubberband` 滤镜进行高质量音频变速
- 最大加速倍数由 `cfg.json` 中 `max_audio_speed_rate` 控制（默认 100%）

**相关配置（videotrans/cfg.json）**：
```json
{
  "max_audio_speed_rate": 100,  // 配音最大加速倍数（100 = 2倍速）
  "max_video_pts_rate": 10      // 视频最大减速倍数（暂未使用）
}
```

**Dubbing.py 调用流程**：
1. 验证声音名称有效性（`get_edge_rolelist()`）
2. 构建 `queue_tts` 列表，`role` 使用原始中文名称（EdgeTTS 内部会转换）
3. 调用 `tts.run()` 生成配音片段（每个字幕一个 wav 文件）
4. 调用 `SpeedRate.run()` 对齐并合并：
   - 检查每段配音时长是否超过字幕时长
   - 超过则使用 ffmpeg atempo 加速（或视频慢放）
   - 合并所有片段为最终配音文件
5. 视频慢放时生成对齐字幕 `aligned.srt`

**注意**：
- `queue_tts` 中的 `role` 必须使用中文名称（如 `晓晓(Female/CN)`），不能使用 VoiceId
- EdgeTTS 内部会调用 `get_edge_rolelist(it['role'], self.language)` 进行转换
- 如果传入已转换的 VoiceId，会导致 `voice must be str` 错误

### 统一配置管理（manager/config.json）

项目使用独立的配置文件 `manager/config.json`，跟随版本控制，在程序启动时自动应用到 videotrans 配置。

**配置文件结构**：
```json
{
  "语音识别": {
    "识别渠道": 0,
    "模型": "medium.en",
    "源语言": "en",
    "分割类型": "all",
    "自动修正": false,
    "LLM重新断句": false,
    "噪音消除": false,
    "最短语音持续_毫秒": 1000,
    "最长语音持续_秒": 10,
    "最短静音持续_毫秒": 1000,
    "语音填充_毫秒": 200,
    "阈值": 0.45,
    "启用VAD": true
  },
  "翻译": {
    "翻译引擎": 0,
    "目标语言": "zh-cn"
  },
  "配音": {
    "配音渠道": 0,
    "声音角色": "晓晓(Female/CN)",
    "语速": "+25%",
    "音量": "+0%",
    "音调": "+0Hz",
    "音频加速": false,
    "视频慢放": true
  },
  "字幕": {
    "字幕类型": 3,
    "中日韩每行字数": 100,
    "其他每行字数": 100
  },
  "处理": {
    "删除间隙静音": false,
    "对齐字幕音频": true,
    "启用CUDA": false
  },
  "输出": {
    "crf": 23,
    "预设": "fast",
    "视频编码": 264
  }
}
```

**配置加载机制**（Config.py）：
1. 程序启动时 `App.py` 调用 `Config.ApplyToVideotrans()`
2. 配置项映射到 `config.params`（任务参数）和 `config.settings`（高级设置）
3. 代码中通过 `Config.Get("配音.语速")` 读取配置

**配置项与 videotrans 变量映射**：
| 配置项 | videotrans 变量 | 位置 |
|--------|-----------------|------|
| 语音识别.模型 | model_name | params |
| 语音识别.自动修正 | auto_fix | params |
| 配音.语速 | voice_rate | params |
| 配音.音频加速 | voice_autorate | params |
| 配音.视频慢放 | video_autorate | params |
| 处理.删除间隙静音 | remove_silent_mid | params |
| 处理.对齐字幕音频 | align_sub_audio | params |
| 处理.启用CUDA | cuda | params |
| 语音识别.阈值 | threshold | settings |
| 语音识别.启用VAD | vad | settings |
| 字幕.中日韩每行字数 | cjk_len | settings |

**代码中直接读取配置**（MainWindow.py）：
```python
Model = Config.Get("语音识别.模型", "medium.en")
AutoFix = Config.Get("语音识别.自动修正", False)
UseCuda = Config.Get("处理.启用CUDA", False)
Voice = Config.Get("配音.声音角色", "晓晓(Female/CN)")
```

---

## 运行方式

### Windows

```bash
# 直接运行
python RunManager.py

# 隐藏窗口启动（双击）
StartManager.vbs
```

### macOS

```bash
# 方法 1：使用 conda run
conda run -n AutoPYVideos python RunManager.py

# 方法 2：激活环境后运行
conda activate AutoPYVideos
python RunManager.py

# 双击启动
Start.command
```

### 开机自启动（macOS）

```bash
# 启用
launchctl load ~/Library/LaunchAgents/com.autopyvideos.plist

# 禁用
launchctl unload ~/Library/LaunchAgents/com.autopyvideos.plist
```

---

## 进度

- [x] 项目结构搭建
- [x] 系统托盘功能
- [x] 单实例管理（VBS 启动 + 窗口唤醒）
- [x] 管理界面（左右分栏 + 日志区域）
- [x] 任务数据模型（文件夹驱动）
- [x] 视频信息获取（yt-dlp + 代理）
- [x] 视频下载模块（断点续传）
- [x] 自动处理队列（同时只处理一个）
- [x] 音频提取模块（Extract.py，调用 videotrans.runffmpeg）
- [x] 语音识别模块（Recognize.py，Faster-Whisper）
- [x] 翻译阶段（Translate.py，调用 videotrans.translator.run）
- [x] 配音阶段（Dubbing.py，edge-tts）
- [x] 合成阶段（Merge.py，调用 videotrans.runffmpeg）
- [x] 启动时校验流程（检测并修复任务信息、已发布任务清理文件）
- [x] 重新执行菜单（未到达阶段置灰、重置时清除发布链接）
- [ ] 发布功能

---

## 常见问题

### edge-tts 配音失败：NoAudioReceived

**问题描述**：配音阶段报错 `edge_tts.exceptions.NoAudioReceived: No audio was received`

**原因**：edge-tts 7.2.2+ 版本存在 bug，导致 TTS 请求失败。参考 [GitHub Issue #443](https://github.com/rany2/edge-tts/issues/443)

**解决方案**：
1. 降级 edge-tts 到 7.2.1：`pip install edge-tts==7.2.1`
2. 中文多语言声音（`zh-CN-XiaoxiaoMultilingualNeural`）在 7.2.1 中不可用，需使用普通声音 `晓晓(Female/CN)`

**注意**：`pyproject.toml` 已锁定 edge-tts 版本为 7.2.1，避免自动升级导致问题
